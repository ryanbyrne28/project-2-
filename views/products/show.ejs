<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/style.css">
    <title>Document</title>
</head>
<body>
    <header>
        <nav class="show">
            <a href="/products/">Home</a>
            
        </nav>
    </header>

    <div class="List">
        <div id="title"><%=product.name %></div>
        <div id="description"><%=product.description %></div>
        <div>Quantity in Stock: <%=product.qty%></div>
        <div id="price">Asking Price: $<%=product.price%></div>
        <img src="<%=product.img %>" alt="product picture">
    </div>
    <form class="buy" method="POST" action="/products/<%= product._id %>/buy">
        <% if(product.qty <= 0){ %>
            <button type="hidden"></button>
            <h4>Out of stock</h4>
        <% } else { %>
            <button id="buy-btn"type="submit">Buy</button>
        <% } %>
        </form>
    <div class="edit">
        <a href="/products/<%= product._id %>/edit"><button type="button" id="edit-btn">Update Watch</button></a>

        <form action="/products/<%= product._id%>?_method=DELETE" method="POST">
            <input id="delete" type="submit" value="Remove Listing" id="delete-btn">
        </form>

        
    </div>

</body>
</html>

<style>

.buy{
    padding: 10px;
}

html{
    padding: 0%;
    margin: auto;
}

body{
    justify-content: center;
    margin-top: 5%;
    align-items: center;
    text-align: center;
}





.list{
    padding-bottom: 10px;
}

input#delete{
    bottom: 0;
    text-align: left;
    position: relative;
    padding: -30px;
    margin-top: 150%;
    margin-left: -10px;
}

li{
    margin: 5px;
}

div#description{
    padding: 10px;
    font-style: italic;
    font-size:x-large;
}

.edit{
    align-items: center;
    align-content: center;
    justify-content: center;
}

a{

    font-size: 135%;
    display: flex;
    justify-content: center;
    text-align: center;
}

    button#buy-btn{
margin: 0 auto;
 --background: green;
--text: #fff;
--cart: #fff;
--tick: var(--background);
position: relative;
border: none;
background: none;
padding-bottom: 10px;
padding: 8px 28px;
border-radius: 8px;
-webkit-appearance: none;
-webkit-tap-highlight-color: transparent;
-webkit-mask-image: -webkit-radial-gradient(white, black);
overflow: hidden;
cursor: pointer;
text-align: center;
min-width: 144px;
color: var(--text);
background: var(--background);
transform: scale(var(--scale, 1));
transition: transform 0.4s cubic-bezier(0.36, 1.01, 0.32, 1.27);
}



button#buy-btn:hover{
background-color: white;
color: green;
}


div#title{
    font-size: 30px;
    margin: 15px;
    font-weight: bold;
}

div#price{
    padding: 5px;
    font-size: 18px;
}

</style>